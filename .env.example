# Copy to .env and fill in real values. Never commit .env.

DB_HOST=localhost
DB_NAME=event_db
DB_USER=root
DB_PASS=
DB_AUTO_MIGRATE=false

MAIL_MODE=log
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=ssl
SMTP_FROM=

CSP_STRICT=false
APP_DEBUG=false
APP_URL=http://localhost
TZ=Asia/Manila

# Optional Certificate of Appearance defaults
# COA_OFFICE_NAME=DICT - Region II
# COA_DEFAULT_VENUE=DICT Regional Office 2
# COA_DEFAULT_PURPOSE=coordinating matters related to the programs and projects of DICT Region 02
